import type { ExtensionRef } from "~/ExtensionRef";

export interface Stat {
  speed: ExtensionRef;
  tears: ExtensionRef;
  damage: ExtensionRef;
  range: ExtensionRef;
  shotSpeed: ExtensionRef;
  luck: ExtensionRef;
}
